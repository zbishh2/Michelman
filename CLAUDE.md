# Michelman — Power BI / BI engagement

LeanGo consulting for Michelman. Power BI (PBIP/TMDL/PBIR), Power Query `.m`, T-SQL, DAX,
custom-visual TypeScript.

## The one rule

Comments and docs say what a thing IS — present tense. No change history, no dates, no
"used to be", no measurement narratives. Git holds history. If something was wrong, it's
just right now.

## Where things are

- Everything is in this repo: `C:\Users\Zack\Documents\Code\Michelman` — outside OneDrive, git works on this machine only, no remote. Nothing syncs to the jumpbox automatically.
- `Cognos Reports\<NN - report>\` holds each rebuild: docs, Txt Queries, XML exports, and the PBIP folder(s) named after the report (variants carry a suffix, e.g. `(SSAS)`). Executive Dashboard and OTIF PBIPs live in `PBIP Projects\`.
- `edw_schema\` holds schema dumps, probe/recon SQL, the snapshot generator, and `ssasprod.bim` (a dump of the STALE `BIQLTabular` model — `Cognos Reports\Extra\v2.xmla` is the truth for `BIQLTabular_v2`).
- Gitignored but present: `Archive/` (junk drawer) and `PBIP Projects\EDW-ODS Snapshot\` (local scratch, 91 tables — generated by `edw_schema/gen_snapshot_pbip.py`; edit the generator, never the TMDL).
- `C:\Users\Zack\Michelman Inc\Power BI - Documents` (outside the repo, SharePoint sync, read-only reference) — Michelman's own report library with hundreds of PBIX files and many paginated RDL reports, plus `General\Data Dictionary` and EDW-developer docs. Check it for precedent before inventing structure; their team uses Report Builder extensively.
## Environment

- Michelman SQL servers (`EDWPROD`/`EDWDEV`/`ODSPROD`) are unreachable from this machine; anything touching them runs on the jumpbox (RDP-only). AD identity: `ZackB@michem.com`.
- EDWDEV's Salesforce chain is live; EDWPROD's is frozen at 2024-08. Complaints reads EDWDEV.
- SSAS production source: `SSASPROD`, model **`BIQLTabular`**. `BIQLTabular_v2` is a modeling/development-like model and is not the production source. Use `edw_schema\ssasprod.bim` for `BIQLTabular` metadata and `Cognos Reports\Extra\v2.xmla` only when intentionally investigating v2. Both are read-only to us; model changes go through Michelman's EDW consultant. ODS is SQL Server — T-SQL, not Oracle.
- Source ladder for new reports (Nick Bubash): **SSAS Live → SSAS Import → EDW Import → ODS Import**. A fallback applies at the report or field level only after the higher source is evaluated and the dependency is documented. Do not retain an EDW/ODS dependency solely to cosmetically reproduce a legacy label when the production SSAS field is adequate. Perspectives only constrain perspective connections; a full-model connection sees base-table columns.
- Preferred SSAS Import shape: use a native DAX query against `SSASPROD` / `BIQLTabular`; define one filtered fact-table variable with `FILTER`; project the required line-grain columns with `SELECTCOLUMNS`; retrieve related attributes with `RELATED`; import raw additive quantity/value columns; and define simple local model measures over those imported columns. Apply stable row-eligibility predicates in the native query so SSAS filters before transfer, while leaving interactive/report filters in the Power BI model. Do not add query-scoped measures or `SUMMARIZECOLUMNS` unless the required output grain actually needs aggregation. Verify every `RELATED` path against `edw_schema\ssasprod.bim` and use model surrogate-key relationships rather than guessed natural keys. `Cognos Reports\19 - 1 - Inventory - Safety Stock and Order Size\19 - Safety Stock and Order Size (SSAS Import)\Safety Stock and Order Size.SemanticModel\definition\tables\Shipments.tmdl` is the validated reference implementation.
- Local SQL Server: `localhost`, Trusted_Connection, databases `EDW` and `ODS` mirror the snapshot under real schema/table names — shipped `.m` runs verbatim with the server swapped to localhost.
- Cloudflare (writeback): token in `writeback/CF Token.txt` (utf-8-sig). The ambient `CLOUDFLARE_API_TOKEN` env var is the wrong account.

## Tools — pick whatever fits, no required order

- `mount-pbip-cache.ps1` — mount a PBIP's `cache.abf`, run DAX via the `powerbi-modeling` MCP at `localhost:<port>`, Desktop closed. Sandbox; can't write back.
- `export-dax-csv.ps1` — uncapped DAX→CSV (MCP caps at 10/100 rows).
- Local SQL mirror (`build-sql-mirror.ps1`) — prove T-SQL correct locally.
- `pbip-shoot.ps1` — render every report page to PNG with cached data, Desktop auto-closed without saving.
- `pbip-validate-drift.ps1` — report lint gated on drift from baseline (`.claude\tools\pbip-baselines\`).
- All in `C:\Users\Zack\.claude\tools\`. Snapshot = data as of last jumpbox refresh; freshness and performance questions still need the jumpbox.

## Hard constraints

- Production PBIPs and `Txt Queries` exports ship comment-free; commented masters live in `edw_model/` and per-report folders.
- TMDL is tab-indented. No `///` above a relationship. Custom-visual GUIDs ≤ 29 chars. Write PBIR files BOM-less.

## Read on demand

- `DEFINITIONS.md` — what OTIF, TRIR, RTFT, Scorecard, Complaints actually are.
- `GOTCHAS.md` — grep when something breaks.
- `writeback/ARCHITECTURE.md` + `writeback/HANDOFF.md` — the writeback system.
- `Cognos Reports/_PROGRESS.md` — rebuild program status.
- `edw_model/EDW_MODEL.md` — Executive Dashboard model handoff.
