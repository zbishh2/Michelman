// Template for visual-revisions/src/config.ts. Copy to config.ts and fill in.
// config.ts is gitignored; this example is committed.

export const WRITEBACK_CONFIG = {
    baseUrl: "https://<worker-subdomain>.workers.dev",
    secret: "<shared secret string>",
};
