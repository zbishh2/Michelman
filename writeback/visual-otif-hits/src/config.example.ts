// Template for visual-otif-hits/src/config.ts. Copy to config.ts and fill in.
// config.ts is gitignored; this example is committed.

export const OTIF_HITS_CONFIG = {
    baseUrl: "https://michelman-writeback.michelman-bi.workers.dev",
    secret: "<shared secret string>",
};
